#pragma once

namespace Engine
{
    struct IRect {
        int x, y, width, height;
    };

    struct FRect {
        float x, y, width, height;
    };

    struct Vec2f {
        float x, y;
    };
}